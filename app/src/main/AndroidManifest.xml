<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dlgsoft.freemarket">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".FreeMarketApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FreeMarket">
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.FreeMarket.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.product.ProductDetailActivity"
            android:theme="@style/Theme.FreeMarket.NoActionBar" />
        <activity
            android:name=".ui.product.ProductPhotoDetailActivity"
            android:theme="@style/Theme.FreeMarket.Transparent" />
        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/app_name"
            android:resource="@xml/searchable" />
    </application>

</manifest>